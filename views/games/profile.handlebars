<article class="profile">
  <h1>{{user.profile.name}}</h1>
  {{#each games}}  
    <article class="game">
        <img src={{this.art}} alt="Game Art">
        <h2>{{this.name}}</h2>
        <p>{{this.keywords}}</p>
        <p>{{this.description}}</p>
    </article>
  {{/each}}
</article>